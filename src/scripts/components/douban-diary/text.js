var debug = require('debug')('app:douban-diary:text');
var React = require('react');

var Home = React.createClass({

  render: function () {

    return (
      <div>douban</div>
    );
  } 
});

module.exports = Home;
